# Contexto de Desarrollo y Asignación de Tareas

## Estado Actual
- **Dispositivo actual:** iMac
- **Enfoque de trabajo activo:** `apps/api` (Backend - NestJS)
- **Flujo Git activo:** [Flujo Multidispositivo](file:///Users/axb/Entregas/flujo-git-multidispositivo.md)
  - `iMac` → desarrollo del Backend (`apps/api`)
  - `Laptop` → desarrollo de Frontend / E-commerce (`apps/frontend`)
- **Regla de Oro Git:** `git pull --rebase origin main` antes de iniciar cada sesión y `git push origin main` al finalizar.

## Servicios en Ejecución
- **Backend API (NestJS):** http://localhost:3001/api
- **ERP Admin (Next.js):** http://localhost:3002
- **E-Commerce Frontend (Next.js):** http://localhost:3000
- **Base de Datos (PostgreSQL Docker):** Puerto `5433` (Contenedor `entregas-db-1`)
